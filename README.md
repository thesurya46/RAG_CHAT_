# RAG_CHAT_
RAG Chat (Retrieval-Augmented Generation Chat) is an AI chatbot that combines information retrieval with generative AI. Instead of relying only on its trained knowledge, it first retrieves relevant documents or data from a knowledge base (like a vector database), then uses that retrieved context to generate more accurate, up-to-date .
